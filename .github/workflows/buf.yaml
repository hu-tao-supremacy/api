name: Test hu-tao-supremacy/homebrew-buf
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          brew list
          brew uninstall go
          brew tap bufbuild/buf
          brew install buf --quiet
